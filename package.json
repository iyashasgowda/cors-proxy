{
  "name": "cors-proxy",
  "version": "1.0.0",
  "description": "CORS Proxy is a NodeJS proxy which adds CORS headers to the proxied request.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yashas Gowda",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.27.2",
    "express": "^4.18.1"
  }
}
